import { TimePassedFormatPipe } from './time-passed-format.pipe';

describe('TimePassedFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TimePassedFormatPipe();
    expect(pipe).toBeTruthy();
  });
});

import { Routes } from '@angular/router';
import { TicketDetailComponent } from './ticket-detail/ticket-detail.component';

export const ticketDetailRoutes: Routes = [
    { path: '', component: TicketDetailComponent }
];
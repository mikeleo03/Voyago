import { Routes } from '@angular/router';
import { TicketsAdminComponent } from './tickets-admin/tickets-admin.component';

export const ticketsAdminRoutes: Routes = [
    { path: '', component: TicketsAdminComponent }
];
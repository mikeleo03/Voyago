import { Routes } from '@angular/router';
import { ToursAdminComponent } from './tours-admin/tours-admin.component';

export const toursAdminRoutes: Routes = [
    { path: '', component: ToursAdminComponent }
];
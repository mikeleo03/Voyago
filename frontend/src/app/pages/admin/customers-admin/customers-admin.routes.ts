import { Routes } from '@angular/router';
import { CustomersAdminComponent } from './customers-admin/customers-admin.component';

export const customersAdminRoutes: Routes = [
    { path: '', component: CustomersAdminComponent }
];
package com.group4.tour.config;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class WebConfigTest {

    @Test
    void contextLoads() {
        // This test checks if the application context loads successfully.
        // If there are any issues with the WebConfig configuration,
        // this test will fail, indicating that the configuration is not correct.
    }
}

